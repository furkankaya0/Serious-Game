using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MissionStart : MonoBehaviour
{
    /*
    private string tag = "Enemy";

    public void Start()
    {
        GameObject[] EnemyObjectsList = GameObject.FindGameObjectsWithTag(tag);
        foreach (GameObject tagged in EnemyObjectsList)
        {
            tagged.SetActive(false);
        }
    }
    public void EnemyAppear()
    {
        GameObject[] EnemyObjectsList = GameObject.FindGameObjectsWithTag(tag);
        foreach (GameObject tagged in EnemyObjectsList)
        {
            tagged.SetActive(true);
            
        }
        Debug.Log("EnemyAppear");
    }

    public void EnemyDisappers()
    {
        GameObject[] EnemyObjectsList = GameObject.FindGameObjectsWithTag(tag);
        foreach (GameObject tagged in EnemyObjectsList)
        {
            tagged.SetActive(false); 
        }

    }*/
}
